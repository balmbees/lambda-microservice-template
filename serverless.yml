service: microservice-template

provider:
  name: aws
  runtime: nodejs8.10
  stage: prod

package:
  artifact: dst.zip

functions:
  hello:
    handler: handlers.hello
    events:
      - http:
          path: hello
          method: get
